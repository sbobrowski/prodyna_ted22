package prodyna.ted22.exercises.ex1

/*
	- Basic Datatypes of Kotlin are Char, String, Double, Float, Int, Long, Short, Byte
 	- Syntax is var / val foobar: String = "Foobar", var = Variable (changeable), val = Value (final). String can be omitted,
 	- Function Syntax is: fun fooBarFunction(test: String) : Unit { println(test) } or just: fun foobarFunction(test: String) = println(test) 
  
   Exercise:
 
 	- Implement function named isPositiveOrNegative which takes a Long as argument and returns a String "{number} is positive" or "{number} is negative"
 	- Implement function named getMax with two Arguments of Type Long returning the greater number
 	- Implement main function calling isPostiveOrNegative with -2 and +2 as argument and calling getMax printing the result
 	- Implement minimum one function as expression, and minimum one with function block 
*/

// Start here (class body not needed in Kotlin, just start with 'fun ...', because let's have fun ;) )